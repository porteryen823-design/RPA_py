# Step Tools - 步驟工具管理系統

## 概述

Step Tools 是一個基於 PyQt5 的桌面應用程式，用於管理和編輯自動化步驟序列。支援專案管理、步驟編輯、右鍵選單操作等功能，並使用 SQLite 資料庫進行資料持久化。

## 主要功能

### 1. 專案管理
- **建立新專案**：支援創建多個專案，每個專案獨立管理步驟
- **專案列表**：在表格中顯示所有專案資訊
- **專案切換**：點擊專案表格行即可切換當前專案
- **專案刪除**：右鍵點擊專案表格可刪除專案（預設專案除外）

### 2. 步驟管理
- **預設步驟**：每個新專案自動建立 100 個步驟（編號 10, 20, 30, ..., 1000）
- **步驟欄位**：
  - 步驟編號：自動編號，唯讀
  - 啟用：勾選框，控制步驟是否執行
  - 動作說明：文字描述
  - 鍵盤動作：勾選框
  - 滑鼠動作：勾選框
  - 座標 X：整數座標
  - 座標 Y：整數座標
  - 持續時間：浮點數，動作持續時間
  - 資料：文字資料
  - 間隔：浮點數，步驟間隔時間
  - 延遲時間：浮點數，延遲執行時間
  - 重複次數：整數，重複執行次數

### 3. 步驟操作
- **新增步驟**：在步驟列表末尾新增步驟
- **刪除步驟**：刪除指定步驟並重新編號剩餘步驟
- **重新編號**：將所有步驟重新編號為 10, 20, 30, ... 的序列

### 4. 右鍵選單功能
#### 步驟表格右鍵選單：
- **增加步驟 (上方)**：在選中步驟上方插入新步驟
- **增加步驟 (下方)**：在選中步驟下方插入新步驟
- **刪除步驟**：刪除選中步驟
- **複製步驟**：複製選中步驟到剪貼簿
- **貼上步驟 (上方)**：在選中步驟上方貼上複製的步驟
- **貼上步驟 (下方)**：在選中步驟下方貼上複製的步驟
- **以上全部 enable**：將選中步驟及以上的所有步驟設為啟用
- **以下全部 disable**：將選中步驟及以下的所有步驟設為停用

#### 專案表格右鍵選單：
- **刪除專案**：刪除選中的專案（預設專案除外）

### 5. 資料持久化
- **SQLite 資料庫**：使用本地 SQLite 資料庫儲存所有資料
- **自動儲存**：點擊儲存按鈕手動儲存資料
- **資料驗證**：輸入驗證和錯誤處理

## 技術架構

### 資料庫結構
```sql
-- 專案表格
CREATE TABLE projects (
    project_id TEXT PRIMARY KEY,
    project_name TEXT NOT NULL,
    create_date TEXT NOT NULL,
    create_user TEXT NOT NULL
);

-- 步驟表格
CREATE TABLE steps (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id TEXT NOT NULL,
    step_no INTEGER NOT NULL,
    enabled BOOLEAN NOT NULL DEFAULT 1,
    action_description TEXT,
    keyboard_action BOOLEAN NOT NULL DEFAULT 0,
    mouse_action BOOLEAN NOT NULL DEFAULT 0,
    coord_x INTEGER,
    coord_y INTEGER,
    duration REAL DEFAULT 0.0,
    data TEXT,
    interval REAL DEFAULT 0.0,
    delay_time REAL DEFAULT 0.0,
    repeat_count INTEGER DEFAULT 1,
    FOREIGN KEY (project_id) REFERENCES projects (project_id),
    UNIQUE(project_id, step_no)
);
```

### 依賴套件
- **PyQt5**：GUI 框架
- **sqlite3**：資料庫操作（Python 標準庫）

### 檔案結構
```
Step_Tools.py          # 主程式檔案
step_tools.db          # SQLite 資料庫檔案（自動生成）
requirements.txt       # 依賴套件列表
```

## 安裝與運行

### 環境需求
- Python 3.6+
- PyQt5

### 安裝步驟
1. 安裝 Python 3.6 或更高版本
2. 安裝依賴套件：
   ```bash
   pip install PyQt5
   ```
3. 運行程式：
   ```bash
   python Step_Tools.py
   ```

## 使用指南

### 基本操作流程
1. **啟動程式**：運行 `python Step_Tools.py`
2. **建立專案**：點擊"建立新專案"按鈕，輸入專案資訊
3. **編輯步驟**：在步驟表格中直接編輯各欄位
4. **儲存資料**：點擊"儲存"按鈕保存變更
5. **使用右鍵選單**：右鍵點擊表格行使用進階功能

### 鍵盤和滑鼠操作
- **編輯欄位**：點擊表格欄位直接編輯
- **勾選框操作**：點擊勾選框切換狀態
- **右鍵選單**：在表格行上右鍵開啟操作選單

### 專案管理
- **切換專案**：點擊專案表格中的任一行
- **刪除專案**：右鍵點擊專案表格行，選擇"刪除專案"
- **新增專案**：點擊"建立新專案"按鈕

## 進階功能

### 批次操作
- **批次啟用/停用**：使用右鍵選單的"以上全部 enable"和"以下全部 disable"
- **批次複製貼上**：複製步驟後可在多個位置貼上

### 步驟編號管理
- **自動編號**：新增/刪除步驟時自動調整編號
- **重新編號**：使用"重新編號"按鈕重設所有步驟編號

### 資料驗證
- **輸入驗證**：自動檢查數值欄位的有效性
- **錯誤處理**：提供友好的錯誤提示訊息

## 故障排除

### 常見問題
1. **程式無法啟動**
   - 確認 Python 版本為 3.6+
   - 確認 PyQt5 已正確安裝

2. **資料庫錯誤**
   - 刪除 `step_tools.db` 檔案重新建立資料庫
   - 檢查檔案權限

3. **介面顯示異常**
   - 確認系統支援中文顯示
   - 檢查 PyQt5 版本相容性

### 日誌和除錯
- 程式在控制台輸出詳細的錯誤資訊
- 可以使用 Python 的 `-c` 選項進行模組測試

## 開發資訊

### 程式架構
- **StepToolsApp 類別**：主應用程式類別
- **MVC 模式**：模型（資料庫）、視圖（PyQt5 介面）、控制器（事件處理）

### 擴展功能
- 可輕鬆添加新的步驟欄位
- 支援自訂右鍵選單功能
- 可擴展為支援更多專案類型

### 版本歷史
- **v1.0**：基本專案和步驟管理功能
- **v1.1**：添加右鍵選單和批次操作
- **v1.2**：增加持續時間、資料、間隔欄位

## 授權與支援

本程式為開源軟體，遵循 MIT 授權條款。如有問題或建議，請聯繫開發者。