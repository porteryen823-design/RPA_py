# Screen Tools - 螢幕工具程式

## 功能概述

`screen_tools.py` 是一個功能完整的螢幕工具程式，提供螢幕擷取、標記、測量和 OCR 文字辨識等功能。現在支援**視窗縮放顯示**功能，將視窗調整為原始解析度的 0.8 倍，同時保持使用原始座標系統進行精確操作。

## 主要功能

### 1. 螢幕顯示與互動
- **視窗縮放顯示**：視窗大小為原始螢幕解析度的 0.8 倍，提供更好的可操作性
- **雙座標系統**：視覺顯示使用縮放圖片，但所有座標記錄使用原始螢幕座標
- 滑鼠移動時顯示十字線（長度 100 pixel，可切換顏色）
- 十字線旁邊即時顯示原始座標 (x, y)，根據螢幕象限調整顯示位置
- 視窗頂部顯示當前座標標籤（顯示原始座標）

### 2. 點標記功能
- **Shift+F1**: 記錄第一點（藍色圓點標記），記錄原始座標
- **Shift+F2**: 記錄第二點（綠色圓點標記），自動繪製紫色矩形，記錄原始座標
- **ESC 鍵**: 反向移除標記點（先移除第二點，再移除第一點）

### 3. 右鍵選單功能
- **1. 標示第一點**: 在滑鼠位置標示藍色圓點，記錄原始座標
- **2. 標示第二點 (並繪出兩點矩形)**: 在滑鼠位置標示綠色圓點並繪製紫色矩形，記錄原始座標
- **3. 存整個螢幕**: 儲存完整的螢幕截圖到 `image_temp/full_screenshot_YYYYMMDDHHMMSS.png`
- **4. 存框出矩形**: 儲存由兩個標記點定義的矩形區域到 `image_temp/rectangle_YYYYMMDDHHMMSS.png`
- **5. 存檔 (帶標記)**: 儲存包含所有標記的圖片到 `image_temp/screenshot_with_marks_YYYYMMDDHHMMSS.png`
- **6. 清除繪製矩形**: 清除所有繪製的矩形和標記點
- **7. 存十字線座標**: 將當前十字線位置的座標存入記憶體（原始座標）
- **8. 重新擷取螢幕**: 重新擷取螢幕並更新底圖（重新計算縮放）
- **9. 滑鼠動作設定**: 開啟 PyQt5 滑鼠動作設定視窗（需要 PyQt5）
- **10. 設定粉紅色十字線**: 將十字線和座標文字設定為粉紅色
- **11. 設定藍色十字線**: 將十字線和座標文字設定為藍色
- **12. OCR 功能**: 當有標記矩形時，進行 OCR 文字辨識（使用原始解析度圖片，需要 Tesseract）
- **13. 隱藏滑鼠設定**: 隱藏滑鼠動作設定視窗
- **14. 傳送資料**: 透過 gRPC 傳送資料到 server
- **15. mouse_action 子選單**: 
  - 移動到滑鼠位置 (coord_x,coord_y)
  - 在目前位置Click
- **16. keyboard_action 子選單**: 
  - 點選目前位置 (coord_x,coord_y)
  - 目前位置全選並清除
  - 目前位置貼上文字
- **17. 存 action_coord**: 儲存當前座標為動作座標（原始座標）

### 4. OCR 文字辨識
- **高準確度 OCR**：使用原始解析度螢幕截圖進行 OCR 辨識，確保最佳準確度
- 支援中文繁體和英文文字辨識
- 辨識結果顯示在置頂的獨立視窗中
- 提供複製到剪貼簿功能
- **翻譯功能**：支援繁體中文、簡體中文翻譯成英文
- 翻譯按鈕位於 OCR 結果視窗中，可手動選擇翻譯方式
- 需要安裝 Tesseract OCR 引擎和 translate 套件

### 5. 滑鼠動作設定 (PyQt5)
- 表格化介面，支援 10 行動作設定
- 可設定動作類型：moveTo, click, dragTo
- 可設定延遲時間和點擊類型
- 支援插入/刪除行功能
- 支援從儲存的座標批量載入
- 支援立即執行設定的動作序列

### 6. gRPC 通訊功能
- 支援與 server 端進行資料交換
- 可傳送座標資料到 server
- 支援滑鼠動作和鍵盤動作的遠端執行
- 自動處理連線失敗情況

## 安裝需求

### Python 套件
```bash
pip install pyautogui pillow tkinter pytesseract translate grpc protobuf
```

### 可選套件（用於進階功能）
```bash
pip install PyQt5  # 用於滑鼠動作設定功能
```

### Tesseract OCR 引擎（用於 OCR 功能）
1. 下載 Tesseract: https://github.com/UB-Mannheim/tesseract/wiki
2. 安裝到預設路徑: `C:\Program Files\Tesseract-OCR\`
3. 或修改程式碼中的路徑設定

## 使用方法

1. 執行程式：
```bash
python screen_tools_client.py
```

2. 程式會顯示縮放為 0.8 倍的螢幕視窗

3. 使用鍵盤和滑鼠進行操作：
   - 移動滑鼠查看十字線和原始座標顯示
   - Shift+F1/F2 記錄點位（記錄原始座標）
   - ESC 移除標記點
   - 右鍵開啟功能選單

4. OCR 使用：
   - 先標記兩個點定義辨識區域（使用原始座標）
   - 右鍵選單選擇 "OCR 功能"
   - 查看辨識結果並可複製到剪貼簿
   - **翻譯功能**：在 OCR 結果視窗中點擊翻譯按鈕
     - "繁體中文翻英文"：指定來源為繁體中文
     - "簡體中文翻英文"：指定來源為簡體中文
     - "自動翻譯"：自動檢測來源語言

5. 滑鼠動作設定：
   - 右鍵選單選擇 "9. 滑鼠動作設定"
   - 在 PyQt5 視窗中設定動作序列
   - 支援載入已儲存的座標
   - 可立即執行或儲存設定

## 檔案輸出

- **完整螢幕截圖**：`image_temp/full_screenshot_YYYYMMDDHHMMSS.png`
- **矩形區域截圖**：`image_temp/rectangle_YYYYMMDDHHMMSS.png`
- **帶標記截圖**：`image_temp/screenshot_with_marks_YYYYMMDDHHMMSS.png`
- OCR 結果：顯示在視窗中，可複製到剪貼簿

## 技術特點

- **視窗縮放技術**：使用 PIL 的 LANCZOS 重採樣算法實現高質量縮放
- **雙座標系統**：視覺顯示與座標計算分離，確保操作精確性
- **Tkinter 圖形介面**：響應式設計，支援事件驅動操作
- **PIL/Pillow 圖片處理**：高效能圖片縮放和操作
- **PyAutoGUI 螢幕擷取**：完整螢幕截圖功能
- **Pytesseract + Tesseract OCR**：高準確度文字辨識
- **PyQt5 進階介面**：專業級表格和按鈕控制項
- **gRPC 網路通訊**：支援分散式操作
- **動態圖形繪製**：即時標記和視覺反饋

## 座標系統說明

### 視窗縮放 vs 原始座標
- **視窗顯示**：使用 0.8 倍縮放的圖片，提供更好的可操作性
- **座標記錄**：所有座標都轉換並記錄為原始螢幕座標
- **OCR 處理**：使用原始解析度截圖，確保最高辨識準確度
- **動作執行**：使用原始座標執行，確保精確定位

### 座標轉換公式
```
原始座標 = int(視窗座標 / 0.8)
視窗座標 = int(原始座標 * 0.8)
```

## 注意事項

- OCR 功能需要額外安裝 Tesseract OCR 引擎
- 程式在 Windows 環境下測試
- 確保螢幕解析度設定正確
- 部分功能可能需要管理員權限
- PyQt5 功能需要正確安裝 PyQt5 套件
- gRPC 功能需要 server 端運行在 localhost:50052

## 版本歷史

- v1.0: 基本螢幕工具功能
- v1.1: 新增 OCR 文字辨識功能
- v1.2: 新增 ESC 鍵反向移除功能和即時座標顯示
- v1.3: 新增十字線座標記憶功能和顏色切換功能，優化座標顯示位置根據螢幕象限
- v1.4: 新增多種螢幕截圖儲存選項（完整螢幕、矩形區域、帶標記圖片）
- v1.5: 新增 OCR 文字翻譯功能，支援繁體/簡體中文翻譯成英文
- v1.6: **重大更新**：新增視窗縮放顯示功能 (0.8倍)，實現雙座標系統，提升 OCR 準確度和用戶體驗
- v1.7: 新增 gRPC 通訊功能、滑鼠動作設定、鍵盤動作設定等進階功能